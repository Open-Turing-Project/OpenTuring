% File "sum2.ti".  Procedure to sum the first n integers.
procedure computeSum (n : int, var sum : int)
    pre n > 0
    % post sum = summation for i from 1 to n of i.
    sum := 0
    for i : 1 .. n
        sum += i
    end for
end computeSum
